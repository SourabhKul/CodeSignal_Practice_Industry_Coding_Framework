# Task Manager

This drill now follows the publicly reported priority, quota, TTL, and overdue-history task-manager family. It is an original practice variant, not a verbatim assessment prompt.

Read one level at a time and implement `solution.py`:

```bash
python3 -m unittest test_solution.TestTaskManager.test_level_1_crud
python3 -m unittest test_solution.TestTaskManager.test_level_2_search
python3 -m unittest test_solution.TestTaskManager.test_level_3_quota_ttl
python3 -m unittest test_solution.TestTaskManager.test_level_4_lifecycle
```
